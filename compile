#!/bin/bash

clear

rm bin/compiled

python3 preprocessor.py "bootloader.asm" src/real_mode_os

cd bin

nasm -f bin output.asm

mkisofs -no-emul-boot -input-charset utf-8 -boot-load-size 60 -exclude-list ../exclude.txt -o os.iso -b output .

touch compiled
